// this can be false if using a fallback (i.e. SPA mode)
export const prerender = true

// built pages are placed in a directory
// ensuring links work as expected
export const trailingSlash = 'always'